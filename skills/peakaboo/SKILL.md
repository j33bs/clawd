---
name: peakaboo
description: Alias adapter for the bundled `peekaboo` skill; use the `peekaboo` CLI for macOS UI capture and automation.
metadata:
  {
    "openclaw":
      {
        "emoji": "👀",
        "os": ["darwin"],
        "requires": { "bins": ["peekaboo"] },
      },
  }
---

# peakaboo (alias)

This workspace adapter preserves compatibility for users who ask for `peakaboo`.

Use the same operational flow as `peekaboo` and execute the `peekaboo` binary.

Quick checks:

```bash
peekaboo --version
peekaboo --help
```
