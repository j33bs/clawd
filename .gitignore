# OpenClaw Security-First .gitignore
# Generated: 2026-02-05
# Category: B (Security)

# ============================================
# SECRETS AND CREDENTIALS (CRITICAL)
# ============================================
secrets.[e]nv
secrets.json
*.key
*.pem
.[e]nv
*.[e]nv.local
*.[e]nv.*.local

# OAuth and auth tokens
auth-profiles.[j]son
device-auth.[j]son
paired.[j]son
pending.json

# Identity files with private-key files
identity/device.[j]son

# Telegram credentials
credentials/telegram-*.json
credentials/*.json

# Telethon session files (MTProto auth)
*.session
*.session-journal
.secrets/

# ============================================
# BACKUP FILES (may contain secrets)
# ============================================
*.bak
*.bak_*
workspace.bak_*/
openclaw.json.bak*
openclaw.json
*.git.bak/

# ============================================
# SESSION DATA (ephemeral)
# ============================================
agents/*/sessions/*.jsonl
sessions.json

# Daily memory logs (ephemeral - local only)
workspace/memory/*.md
workspace/memory/heartbeat-state.json

# ============================================
# RUNTIME STATE (ephemeral)
# ============================================
*.sqlite
memory/*.sqlite
workspace/.state/

# Logs that may contain sensitive data
*.log
telegram_health_log.json
telegram_health_reports.json

# ITC Pipeline runtime state
/telegram/itc_*.json
/telegram/itc_*.jsonl

# ============================================
# TEMP AND CACHE
# ============================================
tmp/
.tmp/
tmp/afk_verify_*.log
tmp/intent_failures_FRESH_*.md
cache/
node_modules/
__pycache__/
*.pyc
.venv/
venv/
.workspace_artifacts/

# Intent failure reports (generated)
workspace/handoffs/intent_failures_*.md

# ============================================
# OS AND IDE FILES
# ============================================
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~
nul

# ============================================
# OPENCLAW RUNTIME DIRECTORIES (managed by gateway)
# Anchored to repo root (/) so workspace/ subdirs are not caught
# ============================================
/agents/
/canvas/
/cron/
/gateway.cmd
/itc/
/market/
/pdf/
/pipelines/
/sim/
/telegram/
/update-check.json

# ============================================
# ONE-OFF DIAGNOSTIC SCRIPTS (workspace)
# ============================================
workspace/system_check_telegram.js
workspace/telegram_health_check.js
workspace/test_tg_connectivity.js
workspace/tg_monitor.ps1
workspace/tg_monitor.sh
workspace/TELEGRAM_MONITORING_FIX.md
workspace/TELEGRAM_MONITORING_SETUP.md
workspace/operator_transfer_brief.md

# ============================================
# CLAUDE CODE SETTINGS (may contain paths)
# ============================================
.claude/
.claude/settings.local.json
.claude/plans/

# Run artifacts (do not commit)
economics/
completions/

# Runtime state/logs (never commit)
workspace/state/
workspace/state/**/*.jsonl

# Local audit scratch (untracked)
workspace/audit/_scratch/

# TeamChat runtime artifacts (local-only)
workspace/teamchat/sessions/
workspace/teamchat/state/
workspace/teamchat/summaries/

# Local append-only audit hook stream
workspace/audit/commit_audit_log.jsonl

# Derived reports are ignored by default; explicitly governed witness artifacts
# can be added intentionally via explicit git add exceptions when needed.
workspace/reports/**

# Probe logs are ignored by default. Exception: dispositional_log.jsonl is a
# governed append-only witness and remains versionable.
workspace/probes/*_log.jsonl
!workspace/probes/dispositional_log.jsonl
workspace/memory/arousal_state.json
workspace/memory/events.json
workspace/memory/relationship.json
workspace/research/insights.json

# Telegram private exports (must remain local-only)
workspace/state_runtime/ingest/telegram_exports/
