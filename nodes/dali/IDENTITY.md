# Dali Identity (Canonical System-1)

- `node.id`: `dali`
- Role: System-1 local-first runtime and primary operator-facing node.
- Legacy aliases (one compatibility cycle): `system1`, `system-1`.

This is the canonical identity file for System-1 naming going forward.
